import React, { useEffect } from "react";

const RedirectQR = () => {
    useEffect(() => {
        window.location.replace("https://www.instagram.com/maskenada_firenze/");
    }, []);
    return(<></>);
}

export default RedirectQR;